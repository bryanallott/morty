<h1>Listing advances</h1>

<table>
  <tr>
    <th>When</th>
    <th>Amount</th>
  </tr>

<% for advance in @advances %>
  <tr>
    <td><%=h advance.when %></td>
    <td><%=h advance.amount %></td>
    <td><%= link_to 'Show', advance %></td>
    <td><%= link_to 'Edit', edit_advance_path(advance) %></td>
    <td><%= link_to 'Destroy', advance, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New advance', new_advance_path %>
